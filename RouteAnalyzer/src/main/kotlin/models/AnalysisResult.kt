package org.example.models

/**
 * this class wrap the different analysis we conducted
 * it is the class which will be written into output.json file
 */
data class AnalysisResult(
    //todo add missing classes
    var mostFrequentedArea: MostFrequentedArea
)